# The Retrieval-as-a-Guardrail app uses only the Python standard library.
# This file exists for readers who scan for one; there is nothing to install.
# See pyproject.toml for the machine-readable version.
